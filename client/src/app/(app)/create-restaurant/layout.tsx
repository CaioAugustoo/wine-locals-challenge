export const metadata = {
  title: "Share Eat | Create Restaurant",
  description: "Create your amazing restaurant and share it with the world!",
};

export default function HomeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
