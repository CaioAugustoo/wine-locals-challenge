export const metadata = {
  title: "Share Eat | New Dish",
  description: "Share Eat is a platform for sharing food with your community.",
};

export default function RestaurantLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
