name: Base Workflow

on:
  pull_request:
    branches: []

jobs:
  server:
    name: Server Workflow
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          path: ./server

      - name: Setup Node
        uses: actions/setup-node@v1
        with:
          node-version: 16.x

      - name: Install dependencies
        run: yarn install

      - name: Lint
        run: cd server && yarn lint

      - name: Format
        run: cd server && yarn format:write && yarn format:check

      - name: Type Check
        run: cd server && yarn typecheck

      - name: Test
        run: cd server && yarn test:coverage

      - name: Build
        run: cd server && yarn build
