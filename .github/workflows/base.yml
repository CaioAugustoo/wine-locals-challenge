name: Base Workflow

on:
  pull_request

jobs:
  server:
    name: Server Workflow
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Get Github Actions current path
        uses: ls

      - name: Run Base Server CI
        uses: ./.github/workflows/base.yml
